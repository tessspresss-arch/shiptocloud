<span class="{{ trim((string) ($wrapperClass ?? '')) }}" role="img" aria-label="{{ $ariaLabel ?? 'MEDISYS Pro' }}">
    <span class="brand-textmark">
        <span class="brand-text">MEDISYS</span>
        <span class="brand-badge">Pro</span>
    </span>
</span>
